# LuckyTag does not load credentials from .env files.
#
# Configure supported model providers in the desktop application's
# "Application Configuration" page. Persistent API keys are stored in the
# macOS Keychain. Optional organization-specific CLIs manage their own login
# sessions; do not copy those credentials into this repository.
